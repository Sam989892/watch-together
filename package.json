{
  "name": "watch-together",
  "version": "0.1.0",
  "private": true,
  "description": "Synced VLC watch-party — sync server, local agent, and React UI",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix agent && npm install --prefix client",
    "dev": "node dev.mjs",
    "server": "npm --prefix server start"
  },
  "engines": {
    "node": ">=22"
  }
}
