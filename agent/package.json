{
  "name": "watch-together-agent",
  "version": "0.1.0",
  "description": "Local agent: bridges your local VLC to the sync server",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
